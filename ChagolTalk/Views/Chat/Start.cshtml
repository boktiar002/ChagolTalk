@{
    ViewData["Title"] = "Find Someone";
    int onlineCount = ViewBag.OnlineCount ?? 0;
    int estimatedWaitSeconds = ViewBag.EstimatedWaitSeconds ?? 15;
    int peopleTalking = ViewBag.PeopleTalking ?? 0;
    bool autoStart = ViewBag.AutoStart ?? false;
}

<div id="lobbyRoot" class="lobby-wrap" data-auto-start="@(autoStart ? "true" : "false")">

    <div class="lobby-card">

        <div id="lobbyError" class="lobby-error"></div>

        <div id="idleState" class="idle-state">

            <div class="lobby-orb">🎙️</div>

            <h1>Ready to talk?</h1>
            <p class="lobby-sub">One tap and we&rsquo;ll find you someone.</p>

            <div class="live-stats">
                <span><span class="live-dot"></span> <span id="onlineCount">@onlineCount</span> online</span>
                <span>&#9201; ~<span id="estimatedWait">@estimatedWaitSeconds</span>s</span>
                <span>&#128172; <span id="peopleTalking">@peopleTalking</span> talking</span>
            </div>

            <button id="startButton" type="button">
                Start Talking
            </button>

            <div id="micCheck" class="mic-status">
                <span class="mic-status-dot"></span>
                <span id="micStatusText">Checking microphone&hellip;</span>
            </div>

        </div>

        <div id="searchingState" class="searching-state">

            <div class="searching-orb"></div>

            <h3>Finding someone for you&hellip;</h3>
            <p id="searchingSub">This usually only takes a few seconds.</p>

            <button id="cancelSearchButton" type="button">Cancel</button>

        </div>

    </div>

</div>

@section Scripts {
    <script src="~/lib/microsoft/signalr/dist/browser/signalr.min.js"></script>
    <script src="~/js/lobby.js" asp-append-version="true"></script>
}
